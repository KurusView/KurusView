#ifndef KURUSVIEW_DARK_MODE_DETECT_H
#define KURUSVIEW_DARK_MODE_DETECT_H

/**
 * @brief detects if dark mode is enabled
 * @return true if enabled
 *
 * @note windows-only support at the moment.
 */
bool isDark();

#endif //KURUSVIEW_DARK_MODE_DETECT_H
